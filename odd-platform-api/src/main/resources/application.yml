spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/odd-platform
    username: odd-platform
    password: odd-platform-password
  jooq:
    sql-dialect: postgres
  codec:
    max-in-memory-size: 20MB
  ldap:
    urls: ""
    dn:
      pattern: ""
#  redis:
#    host: localhost
#    port: 6379
#    username:
#    password:
#    database: 0
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: 'https://cognito-idp.eu-central-1.amazonaws.com/eu-central-1_SLj42PGZ4'
      client:
        registration:
          cognito:
            client-id: '5d2suhs8kcbictf17r960lbd1l'
            client-secret: '3etnhms3o54ni59dmf3hnim6e4qg4iibhvgbrdp5bv74i3p5vru'
            scope:
              - openid
            redirect-uri: 'http://localhost:8080/login/oauth2/code/cognito'
            client-name: 'auth-client'
          github:
            client-id: 'f792bc97df55e97766ad'
            client-secret: 'abd86d7834f0fbd1c0b3baf886f56d22464a5f5b'
            scope:
              - user:read
              - read:org
          google:
            client-id: '832184027882-b0advma5u8cmerak61rvufgkfhr62k1g.apps.googleusercontent.com'
            client-secret: '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'
            scope:
              - openid
              - profile
              - email
        provider:
          cognito:
            issuerUri: 'https://cognito-idp.eu-central-1.amazonaws.com/eu-central-1_SLj42PGZ4'
            user-name-attribute: 'username'
            admin-principals:
            admin-groups: admin
          github:
            user-name-attribute: 'login'
            organization-name: 'damirtestorg'
            admin-principals:
            admin-teams: admin,member
          google:
            user-name-attribute: 'name'
            admin-principals:
            allowed-domain: 'oddp.dev'
#
#cognito:
#  enabled: true
#  logoutUrl:

session:
  # INTERNAL_POSTGRESQL, REDIS, IN_MEMORY
  provider: IN_MEMORY

auth:
  # DISABLED, LOGIN_FORM, OAUTH2, LDAP
  type: OAUTH2

  # For dev/demo purposes only -- username1:password1,username2:password2,etc
  login-form-credentials: admin:admin,root:root

  # For dev purposes only -- successful auth redirect URI
  login-form-redirect:
  ingestion:
    filter:
      enabled: false

metrics:
  export:
    enabled: false
    otlp-endpoint: http://localhost:4317

notifications:
  enabled: false
  message:
    downstream-entities-depth: 1
  wal:
    advisory-lock-id: 100
    replication-slot-name: odd_platform_replication_slot
    publication-name: odd_platform_publication_alert
#  receivers:
#    slack:
#      url:
#      platform-base-url:
#    webhook:
#      url:

management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include: health, prometheus, env, info
  endpoint:
    health:
      enabled: true
    prometheus:
      enabled: true
    env:
      enabled: true
    info:
      enabled: true
  health:
    ldap:
      enabled: false
    redis:
      enabled: false

logging:
  level:
    org.springframework.transaction.interceptor: info
    org.jooq.tools.LoggerListener: info
    io.r2dbc.postgresql.QUERY: info
    io.r2dbc.postgresql.PARAM: info
    org.opendatadiscovery.oddplatform.notification: info

odd:
  activity:
    partition-period: 30

