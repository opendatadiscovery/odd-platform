auth:
  type: DISABLED
  ingestion:
    filter:
      enabled: false

housekeeping:
  enabled: false

datacollaboration:
  enabled: false

metrics:
  storage: INTERNAL_POSTGRES

logging:
  level:
    org.springframework.transaction.interceptor: debug
    org.jooq.tools.LoggerListener: debug
    io.r2dbc.postgresql.QUERY: debug
    io.r2dbc.postgresql.PARAM: debug
    org.opendatadiscovery.oddplatform.service.ingestion: debug