import { Grid } from '@mui/material';
import styled from 'styled-components';

export const ReportSkeleton = styled(Grid)(() => ({ height: '28px' }));
export const SkeletonStatItem = styled(Grid)(() => ({ height: '17px' }));
