configure([project(':odd-platform-api-contract'), project(':odd-platform-api')]) {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }
}

ext {
    env = findProperty('env') ?: 'prod'
}